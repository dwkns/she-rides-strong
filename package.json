{
  "name": "tmp",
  "version": "0.0.2",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@portabletext/to-html": "^2.0.14",
    "@sanity/client": "^6.28.1",
    "@sanity/image-url": "^1.1.0",
    "@tailwindcss/cli": "^4.0.14",
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/typography": "^0.5.16",
    "dotenv": "^16.4.7",
    "eleventy-plugin-console-plus": "^0.1.1",
    "eleventy-plugin-tailwindcss-4": "^1.0.11",
    "tailwindcss": "^4.0.14",
    "touch": "^3.1.1"
  },
  "type": "module",
  "scripts": {
    "clean": "rimraf dist build",
    "dev": "eleventy --serve --quiet",
    "build": "eleventy",
    "sanity:build": "cd sanity && npx sanity build ../src/admin/ -y && cd .. && yarn build",
    "sanity:dev": "cd sanity && sanity dev",
    "deploy": "cd sanity && npx sanity build ../src/admin/ -y && cd .. && yarn build && git add . && git commit -m 'code deploy' && git push"
  },
  "author": "dazza"
}
